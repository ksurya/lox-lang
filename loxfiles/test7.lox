// functions


fun sayHi(first, last) {
    print "Hi " + first + " " + last + "!";
}

fun fib(n) {
    if (n <= 1) return n;
    return fib(n-1) + fib(n-2);
}

fun fact(n) {
    if (n == 0) return 1;
    return n * fact(n-1);
}

sayHi("Alan", "Turing");
print fact(5);
